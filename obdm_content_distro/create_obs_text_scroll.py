import os, sys
import datetime



def get_date_from_path(past_date):
    """
    Create a function that takes 1 parameter that generates the date from NOW - PATE_DATE = DESIRED_TIME
    There's a few different ways we can do this.
    :param past_date:
    :return: DESIRED_TIME
    """
